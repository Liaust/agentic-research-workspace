# Research-map protocols

The versioned protocol documents constrain semantic workers used by the
deterministic coordinator. The current production protocols are under
[`v1/`](v1/); bounded experimental variants are under
[`experiments/`](experiments/).

Protocols are instructions, not evidence. Their outputs enter durable state
only after schema, provenance, evidence-reference, identity, and lifecycle
checks performed by the coordinator.
